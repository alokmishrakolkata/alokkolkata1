@isTest
private class LicensesTabControllerTest {

    static testMethod void cont() {
        
        LicensesTabController con = new LicensesTabController();
        con.reDirect();
        
    }
}